[pytest]
; Adds this folder (faq_chatbot/) to Python's import path when running
; tests, so files inside tests/ can do `from faq_engine import ...` even
; though faq_engine.py lives one folder up from them.
pythonpath = .
